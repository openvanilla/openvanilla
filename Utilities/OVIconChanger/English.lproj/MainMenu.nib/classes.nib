{
    IBClasses = (
        {
            ACTIONS = {
                changeIcon = id; 
                convertIcon = id; 
                homepage = id; 
                openIcon = id; 
                openIconFolder = id; 
                previewIcon = id; 
            }; 
            CLASS = Delegate; 
            LANGUAGE = ObjC; 
            OUTLETS = {listview = id; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}