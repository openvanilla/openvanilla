{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {clearButton = id; relaunch = id; }; 
            CLASS = MyDelegate; 
            LANGUAGE = ObjC; 
            OUTLETS = {console = id; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}