{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {uninstall = id; }; 
            CLASS = UninstallerDelegate; 
            LANGUAGE = ObjC; 
            OUTLETS = {listview = id; resetCheckbox = id; window = id; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}