HDRDIR=Headers
HEADERS=OpenVanilla.h OVLibrary.h OVUtility.h
TARGET_DIR=$(CURDIR)/debian/openvanilla-dev/usr/include/OpenVanilla

configure:

build:

clean:

install:
	for file in $(HEADERS) ; do cp $(HDRDIR)/$$file $(TARGET_DIR) ; done;

